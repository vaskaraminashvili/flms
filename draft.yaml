models:
  Student:
    class_id: integer nullable
    section_id: integer nullable
    name: string
    email: string
  Classes:
    name: string
  Section:
    name: string
    class_id: iteger nullable
